include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
  errors:
    # Treat unused imports as warnings so the skeleton stays tidy.
    unused_import: warning

linter:
  rules:
    prefer_const_constructors: true
    prefer_final_locals: true
    avoid_print: true
